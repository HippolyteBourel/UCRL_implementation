import scipy as sp
import numpy as np
import math
import random

def bellman(old, probs, rewards):
	# to do, juste recuperer github pour gain de temps
	return 0

def value_iteration(probs, rewards, epsilon = 0.01, maxIter = 1000):
	# to do pour gain de temps reprendre implem github
	return 0